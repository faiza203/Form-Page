<!DOCTYPE html>
<html lang="en">

<head>
    {{>links}}
    <link rel="stylesheet" href="css/form.css">
    <title>{{title}}</title>
</head>

<body>
    <div class="main">
        <div class="outer">
            {{>navbar}}
            <h1>{{title}}</h1>
        </div>
        {{>form}}
    </div>
</body>

</html>